# Getting Started

This document describes the getting started guidance for LibreAssistant Mobile.

## Summary
- Clone the repository and enable Corepack for pnpm-based workspace tooling
- Install dependencies and run `pnpm typecheck && pnpm lint && pnpm test && pnpm build`
- Start mobile/server components from workspace filters as needed
- Review privacy, security, and architecture docs before extending core behavior
