# Privacy

This document describes the privacy guidance for LibreAssistant Mobile.

## Summary
- Local-first storage for notes, reminders, and assistant history by default
- API requests only happen when a cloud provider is explicitly configured by the user
- API keys are stored only via secure key storage abstractions
- Telemetry is disabled by default and must be explicitly opted in
