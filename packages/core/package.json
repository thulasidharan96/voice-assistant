{
  "name": "@libreassistant/core",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src test --ext .ts",
    "test": "vitest run"
  },
  "dependencies": {
    "@libreassistant/ai": "workspace:*",
    "@libreassistant/plugins": "workspace:*",
    "@libreassistant/security": "workspace:*",
    "@libreassistant/storage": "workspace:*"
  }
}
