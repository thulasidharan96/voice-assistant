import React from 'react';
import { Text } from 'react-native';
import { Card, Screen } from '@libreassistant/ui';

export function NoteEditorScreen(): JSX.Element {
  return (
    <Screen>
      <Card>
        <Text style={{ color: '#F4F7FF' }}>Note editor placeholder</Text>
      </Card>
    </Screen>
  );
}
