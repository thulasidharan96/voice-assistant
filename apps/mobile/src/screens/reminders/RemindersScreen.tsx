import React from 'react';
import { Text } from 'react-native';
import { Card, Screen } from '@libreassistant/ui';

export function RemindersScreen(): JSX.Element {
  return (
    <Screen>
      <Card>
        <Text style={{ color: '#F4F7FF' }}>Reminders list placeholder (notification-ready architecture)</Text>
      </Card>
    </Screen>
  );
}
