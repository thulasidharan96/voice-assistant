import React from 'react';
import { Text } from 'react-native';
import { Card, Screen } from '@libreassistant/ui';

export function ReminderEditorScreen(): JSX.Element {
  return (
    <Screen>
      <Card>
        <Text style={{ color: '#F4F7FF' }}>Reminder editor placeholder</Text>
      </Card>
    </Screen>
  );
}
